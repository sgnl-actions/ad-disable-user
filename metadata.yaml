name: ad-disable-user
description: Disable an enabled user account in on-premise Active Directory via LDAP

# Input parameters for web form generation
inputs:
  userDN:
    type: text
    description: Distinguished Name of the user to disable (e.g., CN=John Doe,OU=Users,DC=corp,DC=example,DC=com)
    required: true
    validation:
      min: 1
      max: 500

  address:
    type: text
    description: Optional LDAP server URL override (e.g., ldaps://ad.corp.example.com:636)
    required: false
    validation:
      min: 1
      max: 200

# Expected output structure
outputs:
  status:
    type: text
    description: Operation result (success, failed, etc.)

  userDN:
    type: text
    description: Distinguished Name of the user that was processed

  disabled:
    type: boolean
    description: Whether the user was newly disabled (false if already disabled)

  previousUAC:
    type: number
    description: The userAccountControl value before the operation

  newUAC:
    type: number
    description: The userAccountControl value after the operation

  address:
    type: text
    description: The LDAP server URL that was used
